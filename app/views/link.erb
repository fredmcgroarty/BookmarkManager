<head>
  <link rel="stylesheet" type="text/css" href="../css/reset.css">
  <link rel="stylesheet" type="text/css" href="../css/application.css">
</head>
<li class='link'>
  <a class='title' href="<%= link.url %>"> website:<%= link.title %></a>
    <div class='description'>
      <% if link.description.empty? %>
        <p>A description has not been added.</p>
      <% else %>
        <div class="description"><p>description: <%= link.description %>  <span class='source'>
      <% end %>
        <div class="link_info">added: @ <%= link.time.strftime("%T on %d-%m-%Y") %> by <a href="/profile/<%= link.user.username %>"><%= link.user.email %></a>
        </div>
<button type="button">&#9733;</button>

  </span></p>
  </div>
  <div class="tags"> tags: 
		<% link.tags.each do |tag| %>
			 <a href="/tags/<%= tag.text %>"><%= tag.text %></a>
    <% end %>
	</div>
</li>